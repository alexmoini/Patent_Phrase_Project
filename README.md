# Patent_Phrase_Project

Improving transformers using patent phrase similarity

Running ./run.sh after creating the conda environment from the environment.yml file will download files from PMC AWS S3 bucket to the local machine. A Test run will download a small fraction of the overall data (~1/100,000th), while a Full run will download the complete open access directory.
